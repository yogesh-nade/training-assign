import { MedicineFilterPipe } from './medicine-filter-pipe';

describe('MedicineFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicineFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
